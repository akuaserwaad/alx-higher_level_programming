#!/usr/bin/python3
def square_matrix_simple(matrix=[]):
    if matrix = []:
        return matrix
    new_matrix = len(matrix)
    for i in matrix:
        new_matrix = matrix.append(map(lambda x: x**2, i))
    return new_matrix
